# 项目规则（本文件优先级高于个人偏好与习惯写法）

> 本文件是**硬性约束**，不是建议。新增任何功能前先读第 2 节，提交前对照第 4 节自检。
> 完整规范与代码示例见 [`docs/design/20-扩展模块架构规范.md`](docs/design/20-扩展模块架构规范.md)。

---

## 1. 唯一规则：一切功能皆扩展模块，不用就不存在

这个项目会持续塞进功能，几十上百个。因此**功能数量不得转化为运行负担**：
每个功能都是一个**扩展模块**，只有用户真正进入它的页面时才初始化；
没启用的模块，连它的代码路径都不执行。

判断标准只有一条，且必须可验证：

> **把一个功能模块的代码整体去掉，程序的行为与启动耗时不应有任何可观察差别**（除非你恰好进入那个页面）。

「不加载」在本项目里是**可证伪的技术承诺**，不是形容词。

---

## 2. 硬性约束

### 2.1 必须遵守

| # | 约束 |
| --- | --- |
| 1 | **新功能 = 新模块**。不允许把新功能塞进已有模块的页面或 `App` 结构体里。 |
| 2 | 每个模块必须在 `app.go` 的 `registerModules()` 中注册：`Meta`（`ID`/`Name`/`Desc`/`Icon`/`Status`/`DefaultEnabled`）+ `OpenFn`。模块 id 为小写单词（`chat`/`disk`/`deploy`/`javadev`）。 |
| 3 | 模块业务逻辑放 `internal/<id>/`，是**纯 Go 包**：不 import `wails`、不 import `main`、不读全局状态，可独立单测。 |
| 4 | `OpenFn` 只做该模块自身的轻量初始化（如装载自己的规则文件），**必须毫秒级**。文件系统遍历、进程/端口探测、网络请求、子进程启动一律**不得**出现在 `OpenFn`，只能由用户在页面上的显式动作触发。 |
| 5 | 模块的 IO、探测、goroutine、事件订阅，**禁止**出现在：包级 `var` 初始化、`init()`、`startup()`、`NewApp()`、`registerModules()`。 |
| 6 | 前端一个模块 = 一个 `<section id="page-<id>">` + 一个幂等的 `init<Id>()`；只由 `openPage(id)` 在首次进入时调用。事件订阅、轮询定时器只能写在 `init<Id>()` 里——模块被禁用时它们**一次都不会被创建**。 |
| 7 | 模块对前端的类型必须定义在 `main` 包（DTO）。Wails 运行时会**静默丢弃**签名直接引用 `internal/...` 包类型的绑定方法（已实测，见 `app.go:211`）。 |
| 8 | 模块的第三方依赖不得带包级 `init` 副作用（注册全局连接、开后台协程、写临时文件）。引入新依赖时确认这一点。 |
| 9 | 模块被禁用后必须回到「从未加载」状态：`Registry.SetEnabled(id,false)` 已重置 `opened`，重新启用再进入要重新走懒加载。模块自身要保证退出/禁用时不留孤儿 goroutine、子进程、文件句柄。 |
| 10 | 保持单进程、无后台驻留：不注册开机自启、不常驻服务。这里「不加载」的含义是**初始化代码与 IO 不发生**，而不是「没有进程」——不要用这句话给自己开后门，也不要假装有独立进程。 |

### 2.2 禁止的写法

- 「顺手」在 `startup()` 里给某模块预热数据、建立连接、探测工具链。
- 在 `App` 结构体上直接挂某模块的运行时状态，并在 `NewApp()` 里初始化它。
- 把两个不相关功能合成一个模块页面（「三合一」「顺手加个页签」）。
- 页面加载即启动轮询/定时器，即使用户没在用。
- 用「反正很便宜」为理由省掉懒加载——便宜是测量出来的，不是假设的。

---

## 3. 新增一个模块

1. `internal/<id>/`：纯逻辑包 + 单测。
2. `<id>_api.go`（根目录，`main` 包）：`App` 上的绑定方法，DTO 定义在 `main` 包。
3. `app.go` 的 `registerModules()`：注册 `Meta` + `OpenFn`（遵守约束 4）。
4. 前端：`page-<id>` 容器、`PAGE_META` 条目、`init<Id>()`、`openPage()` 分派、`renderRail` 会自动带上它。
5. 配置：`modules.<id>.enabled` 走 `DefaultEnabled` 默认值；若可作为主页候选，`general.home` 可指向它。
6. `make test` + `make vet` 通过；按第 4 节自证「不加载」。

---

## 3.5 基础规则：一切能力皆可被 AI 调用（对外接口）

> 完整规范与统一契约见 [`docs/design/22-对外能力接口与AI可控制规范.md`](docs/design/22-对外能力接口与AI可控制规范.md)。

### 3.5.1 硬性约束

| # | 约束 |
| --- | --- |
| 1 | **每个已注册模块必须至少暴露一个对外 action**。漏了说明有功能只能从界面点、AI 控制不了——由 `TestEveryModuleHasAction` 强制 |
| 2 | 每个动作必须有 `summary`（给 AI 判断该不该调）、必填参数必须有 `example`——由 `TestEveryActionIsDescribed` 强制 |
| 3 | **会删/移数据的动作必须标 `destructive`**——未确认时注册表直接拒绝且不执行 handler，由 `TestDestructiveRequiresConfirm` 强制 |
| 4 | 动作签名直接引用 `internal/...` 包类型的绑定方法会被 Wails 静默丢弃（同第 7 条），因此 API 动作的参数与返回**只用 main 包 DTO** |

### 3.5.2 默认形态：CLI/stdio（零常驻，与「不常驻服务」规则兼容）

```
助手.exe api list                       列出全部能力
助手.exe api describe <action>          查看参数说明与示例
助手.exe api call <action> --json '{...}'   调用（AI 一条命令即一次调用）
```

- **零常驻**：不起监听、不占端口、进程退出即结束
- **状态不跨进程**：每个 `api call` 是新进程——因此长操作必须提供**同步阻塞**动作（如 `disk.scan` 调用即等待完成），轮询类动作只服务于长驻服务模式

### 3.5.3 可选：本机 HTTP 监听

只在需要订阅进度、被外部编排器长连接时使用。`api.enabled: true` 才启动，**默认关闭**、只绑 `127.0.0.1`。当前尚未实现。

### 3.5.4 危险分级

| 级别 | 行为 |
| --- | --- |
| `readonly` | 只读，不改任何东西 |
| `mutating` | 会改状态（写文件、启动进程） |
| `destructive` | **会删数据**：默认拒绝，必须显式确认（`--confirm`），且动作内部仍走 dry-run 优先 |

### 3.5.5 新增能力时

1. 在 `api_actions.go` 的 `register<X>Actions` 里注册 action（含 summary、params、kind）
2. 危险动作标 `destructive`
3. `make test` 通过（三条强制测试会自动检查你的模块）

---

## 4. 提交前自检：怎么证明它真的没被加载

- **启动耗时**：标题旁显示的 `启动 X ms` 不因新增模块而变化（差值应在噪声内）。
- **零副作用**：只启动程序、**不进入该模块页面**，观察不到该模块的日志、网络请求、子进程、文件遍历。
- **可禁用性**：把 `modules.<id>.enabled` 设为 `false` → 导航不出现该页，`EnterModule` 被拒（返回可读原因），程序其余部分正常工作。
- **可摘除性**：注释掉该模块的注册与页面，程序仍能编译运行——做不到就说明耦合泄漏了。
- 有条件的模块补一条测试：断言「未 `Open` 的模块，其 `OpenFn` 未被调用」。

---

## 5. 现存欠账（本次不动，后续任务按上述规则改造）

| 位置 | 违约点 | 目标形态 |
| --- | --- | --- |
| `internal/deploy`、`app.go` 的 `disk` 模块 | `disk` 是「扫描 / 清理 / 迁移」三合一，一个模块塞三个功能 | 拆为 `scan` / `clean` / `migrate` 独立模块，可各自禁用 |
| `deploy` 模块（Java 开发） | 一个模块页里放了「服务启停 / Git 变更 / 打包 / 配置」四件事——开发与打包共用同一份项目清单，暂时合在一页 | 若将来开发态与打包态的配置真的分家，再拆成 `javadev` / `deploy` 两个模块；决策与理由见 `docs/design/21` |
| `internal/deploy` 与 `internal/javadev` | 共用 `deploy` 配置（项目与模块清单） | 有意为之（用户不必配两遍项目），见 `docs/design/21` §2.1；真要拆模块时先定配置归属 |

**已还清**（2026-09-15，`javadev` 相关）：

- ✅ 模块已注册进 `registerModules()`（id `deploy`，名字「Java 开发」），前端 `page-deploy` 有对应页面
- ✅ `OpenFn` 只做一次函数指针赋值（接事件总线），毫秒级；探测、端口查询、git 查询全部由用户显式动作触发
- ✅ 服务运行时改为首次使用时创建（`devMgr()`），不再是包级 `var`
- ✅ 绑定方法签名只用 `main` 包 DTO；模块被禁用时停掉它启动的服务（不留孤儿进程）

改造原则：**一次完成一个模块，改造后按第 4 节验收**；不要为了「一次性对齐规则」做出大范围重构而破坏安全边界（清理/迁移路径的全部安全约束优先于本规则）。
